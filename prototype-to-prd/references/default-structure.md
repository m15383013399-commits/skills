# Default Page Structure

在项目没有单独定义结构时，默认按下面结构写每个页面或功能点：

1、页面/功能点
2、页面结构
3、交互说明
4、字段与规则
5、状态说明
6、异常与边界
7、本期不做 / 待确认

补充规则：

1、页面结构写区域和字段，不写空泛介绍。
2、交互说明优先写按钮、切换、弹窗、返回路径。
3、字段与规则优先写必填、默认值、可编辑性、联动、校验。
4、状态说明优先写默认、空、禁用、完成、异常。
5、异常与边界优先写拦截条件、提示文案、覆盖逻辑、历史影响。
